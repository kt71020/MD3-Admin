# 常用命令

## 檢查

```cmd
kubectl -n adm-md3-web get deploy,svc,ingress,pods -o wide | cat
kubectl -n adm-md3-web get certificate,secret | cat
kubectl get clusterissuer | cat
curl -I https://md3-adm.uirapuka.com | cat

```
